name: Test basic upload with asset_name and release options
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: Create a dummy file to upload
        run: echo "hello world" > /tmp/test-asset.txt
      - id: action
        uses: .
        continue-on-error: true
        with:
          repo_token: fake-token-for-testing
          file: /tmp/test-asset.txt
          tag: refs/tags/v0.0.1-test
          asset_name: test-asset.txt
          overwrite: "true"
          release_name: "Test Release"
          body: "This is a test release body."
          make_latest: "false"
          target_commit: main
          repo_name: ${{ github.repository }}
          check_duplicates: "true"
      - name: Assert action ran (may fail due to fake token)
        run: |
          echo "Action completed (continue-on-error=true)"
          echo "PASS: inputs were accepted"
